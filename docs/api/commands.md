# Command catalog

GENERATED by tools/gen_api_docs.py -- do not edit by hand; rerun the
script after changing crates/app/src/commands.rs and commit the result.

Every `#[tauri::command]` in commands.rs. **pure** commands read only
their arguments and the static data packs -- they are the first
candidates for exposure outside the Tauri shell. **stateful** commands
read the live session (`AppState`: the ingest store, config, status)
and need the service layer described in README.md before external
exposure. Parameters shown are the real API surface -- Tauri plumbing
(`State`, `AppHandle`, `Window`) is elided.

113 commands: 24 pure, 89 stateful.

## `get_character_estimate` (pure)
why: Character Planner's one call -- full attribute sheet + mana estimate; `gear` summed by the frontend, this side never touches an item. Stateless on purpose -- nothing persisted, fresh launch starts blank.

- args: `race: String`; `classes: Vec<String>`; `class_levels: Vec<u8>`; `gear: Option<std::collections::HashMap<String, f64>>`
- returns: `Option<CharacterEstimateDto>`

## `get_era_options` (pure)

- args: none
- returns: `EraOptionsDto`

## `get_exalt_candidates` (pure)

- args: `id: String`; `socket_key: String`; `other_assignments: std::collections::HashMap<String, String>`; `classes: Vec<String>`; `max_era: Option<String>`
- returns: `Vec<ItemDto>`

## `get_game_data_meta` (pure)

- args: none
- returns: `GameDataMetaDto`

## `get_gear_recommendations` (pure)
why: top candidates per slot, scored against classes/race; `level` lets INT/WIS score as actual mana value not a flat fallback

- args: `classes: Vec<String>`; `race: Option<String>`; `max_era: Option<String>`; `per_slot: Option<usize>`; `weights: Option<std::collections::HashMap<String, f64>>`; `level: Option<u8>`; `equipped: Option<std::collections::HashMap<String, String>>`; `owned: Option<std::collections::HashMap<String, u32>>`; `owned_tier: Option<std::collections::HashMap<String, u8>>`
- returns: `Vec<SlotRecommendationDto>`

## `get_gear_weights` (pure)

- args: `classes: Vec<String>`; `level: Option<u8>`
- returns: `std::collections::HashMap<String, f64>`

## `get_item_at_tier` (pure)

- args: `id: String`; `tier: u8`
- returns: `Option<ItemDto>`

## `get_item_with_exalts` (pure)

- args: `id: String`; `tier: u8`; `exalts: std::collections::HashMap<String, String>`
- returns: `Option<ItemDto>`

## `get_mob_aliases` (pure)

- args: none
- returns: `Vec<(String, String)>`

## `get_npc_markers_for_zone` (pure)

- args: `zone: String`
- returns: `Vec<NpcMarkerDto>`

## `get_npc_nav_points` (pure)
why: "set a path to this NPC, same way pick-destination does" -- the player's own ask. Coordinates come from the wiki spawn points the npc-overlay markers already plot; zone resolution reuses the same alias fold `zone_matches` applies everywhere else.

- args: `name: String`
- returns: `Vec<NpcNavPointDto>`

## `get_spell_stacking_groups` (pure)

- args: none
- returns: `HashMap<String, u32>`

## `get_tradeskill_catalog` (pure)
why: static recipe catalog for the Tradeskill module -- every core tradeskill's own recipe list, baked in at compile time, no Ingest needed

- args: none
- returns: `Vec<TradeskillSkill>`

## `get_ui_shell` (pure)

- args: none
- returns: `UiShellDto`

## `get_window_capability` (pure)

- args: none
- returns: `WindowCapabilityDto`

## `list_aa` (pure)

- args: none
- returns: `Vec<aadata::Aa>`

## `list_gear_items` (pure)
why: item browser; `owned`/`owned_tier` are the frontend's already- loaded dump fields passed back in -- this command has no dump of its own

- args: `classes: Vec<String>`; `slot: Option<String>`; `max_era: Option<String>`; `owned: Option<std::collections::HashMap<String, u32>>`; `owned_tier: Option<std::collections::HashMap<String, u8>>`
- returns: `Vec<ItemDto>`

## `list_notification_kinds` (pure)

- args: none
- returns: `Vec<NotificationKindDto>`

## `list_npc_zone_candidates` (pure)
why: NPC-overlay candidate list, toggle-able not auto-applied -- can't reliably resolve wiki names against internal EQ shortcodes

- args: `map_zone_name: String`
- returns: `Vec<String>`

## `list_npcs` (pure)

- args: none
- returns: `Vec<npcdata::Npc>`

## `list_spell_effects` (pure)

- args: none
- returns: `Vec<spelleffect::SpellEffectsEntry>`

## `list_spells` (pure)

- args: none
- returns: `Vec<spelldata::Spell>`

## `list_zone_npcs` (pure)

- args: `map_zone_name: String`
- returns: `Vec<ZoneNpcDto>`

## `list_zones` (pure)

- args: none
- returns: `Vec<zonedata::Zone>`

## `clear_notification_sound` (stateful)
why: picks and copies a sound file in, saves as `kind`'s custom sound; Ok(None) on cancel, not an error, same stance as `pick_log_directory`

- args: `kind: String`
- returns: `Result<settings::NotificationSettings, String>`

## `clear_notification_sound` (stateful)

- args: `kind: String`
- returns: `Result<settings::NotificationSettings, String>`

## `find_existing_inventory_dump` (stateful)

- args: none
- returns: `Option<ExistingInventoryDumpDto>`

## `find_walk_path` (stateful)

- args: `pack: Option<String>`; `zone: String`; `from: [f32; 3]`; `to: [f32; 3]`
- returns: `Result<PathDto, String>`

## `find_zone_route` (stateful)

- args: `from_zone: String`; `to_zone: String`
- returns: `Result<ZoneRouteDto, String>`

## `find_zone_route` (stateful)

- args: `from_zone: String`; `to_zone: String`
- returns: `Result<ZoneRouteDto, String>`

## `get_aa_log` (stateful)

- args: none
- returns: `AaLogDto`

## `get_app_version` (stateful)

- args: none
- returns: `String`

## `get_app_version` (stateful)
why: Info page's own version display -- same source check_for_update's current_version reads, exposed standalone so showing it doesn't need a network round trip. Backend command through invoke(), not a raw @tauri-apps/api call (see invoke.ts's doc).

- args: none
- returns: `String`

## `get_class_configurations` (stateful)

- args: `name: String`
- returns: `ClassConfigurationsDto`

## `get_combat_summary` (stateful)

- args: `zone_visit: Option<i64>`; `encounter_id: Option<u32>`; `actor: Option<String>`; `confirmed_only: Option<bool>`
- returns: `CombatSummaryDto`

## `get_configuration_zone_visits` (stateful)

- args: `name: String`; `classes: Vec<String>`; `level_range: Option<(u8, u8)>`
- returns: `Vec<ZoneVisitDto>`

## `get_craft_log` (stateful)
why: real craft attempts this file has ever recorded, joined against the static catalog above -- see craftlog's own doc

- args: none
- returns: `Vec<CraftLogEntryDto>`

## `get_current_level` (stateful)
why: feeds the Gear Planner's mana weighting; None mostly means "same level the whole file", not "unknown"

- args: none
- returns: `Option<u8>`

## `get_damage_spells` (stateful)

- args: `assume_max_rank: bool`
- returns: `Vec<DamageSpellDto>`

## `get_death_recap` (stateful)
why: "why did I just die" -- see deathrecap.rs's own doc. `death_ts` picks a specific death from the returned list; None means the most recent. Both halves in one call: the recap plus every death timestamp this session, so the frontend's picker never needs a second round trip.

- args: `death_ts: Option<i64>`
- returns: `(Option<crate::deathrecap::DeathRecapDto>, Vec<i64>)`

## `get_default_gear_classes` (stateful)

- args: `name: String`
- returns: `Vec<String>`

## `get_drop_watch` (stateful)
why: Drop Watch widget -- see dropwatch.rs's own doc. Unfiltered (every currently-relevant mob's full known drop list); the frontend intersects against tracked_drop_items, same split get_skill_status uses.

- args: none
- returns: `Vec<crate::dropwatch::DropWatchRowDto>`

## `get_encounter_detail` (stateful)

- args: `encounter_id: u32`
- returns: `Option<EncounterDetailDto>`

## `get_epic_quests` (stateful)
why: Endgame's Epic Quests farm list -- see epicquests.rs's own doc

- args: none
- returns: `Vec<crate::epicquests::EpicClassDto>`

## `get_fight_state_at` (stateful)
What clicking a point on the scrub bar shows: every entity's state and a snapshot DPS reading as of that instant.

- args: `encounter_id: u32`; `ts_ms: i64`
- returns: `Vec<EntityStateDto>`

## `get_fight_timeline` (stateful)
Per-entity damage-over-time bars for one fight's scrub bar.

- args: `encounter_id: u32`
- returns: `Option<FightTimelineDto>`

## `get_game_state` (stateful)

- args: none
- returns: `GameStateDto`

## `get_guild_chat` (stateful)

- args: none
- returns: `Vec<ChatMessageDto>`

## `get_inventory_browser` (stateful)
why: Character module's Inventory tab -- bags/bank/depot/key ring, grouped by real container. Equip-doll slots deliberately excluded, see `inventory::ParsedInventory::containers`' own doc.

- args: none
- returns: `Vec<inventory::InventoryContainerDto>`

## `get_inventory_dump` (stateful)

- args: `file: String`
- returns: `Result<InventoryDumpDto, String>`

## `get_item_loot_history` (stateful)

- args: `item: String`
- returns: `Vec<LootEventDto>`

## `get_last_location` (stateful)

- args: none
- returns: `Option<LastLocationDto>`

## `get_live_meter` (stateful)

- args: none
- returns: `Option<combat::LiveMeterDto>`

## `get_loadout_summary` (stateful)
why: past parses bundled by class combination -- avoids blending playstyles into one number; same treatment as `get_mob_history`

- args: `target: String`; `confirmed_only: bool`
- returns: `Vec<history::LoadoutSummary>`

## `get_map_file` (stateful)

- args: `pack: Option<String>`; `zone: String`
- returns: `Result<mapsdata::MapFileDto, String>`

## `get_mob_history` (stateful)
why: past parses against `target`, newest first; re-resolves loadout against live class evidence before returning, not the as-of-close snapshot

- args: `target: String`; `confirmed_only: bool`
- returns: `Vec<ParseRecord>`

## `get_mob_stats` (stateful)

- args: `mob_name: String`
- returns: `MobStatsDto`

## `get_notification_settings` (stateful)

- args: none
- returns: `settings::NotificationSettings`

## `get_notification_sound_data` (stateful)

- args: `kind: String`
- returns: `Option<String>`

## `get_party_chat` (stateful)

- args: none
- returns: `Vec<ChatMessageDto>`

## `get_planner_state` (stateful)

- args: none
- returns: `PlannerStateDto`

## `get_pm_history` (stateful)

- args: `player: String`
- returns: `Vec<ChatMessageDto>`

## `get_preferences` (stateful)

- args: none
- returns: `Preferences`

## `get_raid_chat` (stateful)

- args: none
- returns: `Vec<ChatMessageDto>`

## `get_raids` (stateful)

- args: none
- returns: `Vec<RaidRowDto>`

## `get_recent_crafts` (stateful)
why: Overview's recently-crafted list, last 15 successful combines

- args: none
- returns: `Vec<craftlog::RecentCraftDto>`

## `get_session` (stateful)

- args: none
- returns: `SessionDto`

## `get_skill_status` (stateful)
why: Skill Tracker widget's own-cooldowns section -- see skilltracker.rs's own doc

- args: none
- returns: `Vec<crate::skilltracker::SkillStatusDto>`

## `get_sky_class_unlocks` (stateful)

- args: none
- returns: `Vec<skyquests::SkyClassUnlockDto>`

## `get_sky_quests` (stateful)
why: "Sky - Quests" tab -- every material turn-in, full detail

- args: none
- returns: `Vec<skyquests::SkyClassDto>`

## `get_spell_check` (stateful)

- args: none
- returns: `SpellCheckDto`

## `get_spell_ranks` (stateful)

- args: none
- returns: `HashMap<String, u8>`

## `get_spellbook` (stateful)

- args: none
- returns: `Vec<SpellbookEntryDto>`

## `get_status` (stateful)

- args: none
- returns: `StatusDto`

## `get_status_effects` (stateful)
why: overlay's timed-effects widget -- same polled-on-tick shape as get_live_meter, see effects.rs's own doc

- args: none
- returns: `crate::effects::StatusEffectsDto`

## `get_target_effects` (stateful)
why: Skill Tracker widget's target-effects section -- see targeteffects.rs's own doc

- args: none
- returns: `crate::targeteffects::TargetEffectsDto`

## `get_tracked_loot_status` (stateful)
why: Drop Watch's "you just got one, remove it?" prompt -- see TrackedLootDto's own doc. `items` is whatever the frontend is currently tracking, one call covers all of them.

- args: `items: Vec<String>`
- returns: `Vec<crate::dropwatch::TrackedLootDto>`

## `get_tradeskill_levels` (stateful)
why: current tradeskill levels off skill-up lines -- see craftlog's own doc

- args: none
- returns: `Vec<craftlog::TradeskillLevelDto>`

## `get_ui_file` (stateful)

- args: `file: String`
- returns: `Result<uifiles::ParsedUiFileDto, String>`

## `get_unmatched_coverage` (stateful)

- args: `top: Option<usize>`
- returns: `UnmatchedCoverageDto`

## `get_zone_context` (stateful)
why: Maps module's zone-identity + entrance-guess input; `current_map_zones` confirms the open map is really current, `previous`/`teleport_landing` decide the guess

- args: none
- returns: `ZoneContextDto`

## `list_all_map_zones` (stateful)

- args: none
- returns: `Vec<String>`

## `list_allies` (stateful)

- args: `zone_visit: Option<i64>`; `encounter_id: Option<u32>`; `confirmed_only: Option<bool>`
- returns: `Vec<AllyDto>`

## `list_debug_encounters` (stateful)

- args: `limit: Option<usize>`
- returns: `Vec<DebugEncounterDto>`

## `list_encounters` (stateful)
why: Combat module's second dropdown, defaults to the whole list -- a rendering cost the frontend virtualizes, not a fetch one. `zone_visit`: None = no filter, -1 = "Unknown" bucket, else a visit index.

- args: `zone_visit: Option<i64>`; `offset: Option<usize>`; `limit: Option<usize>`
- returns: `Vec<EncounterDto>`

## `list_map_packs` (stateful)

- args: none
- returns: `Vec<String>`

## `list_map_zones` (stateful)

- args: `pack: Option<String>`
- returns: `Vec<String>`

## `list_mob_encounters` (stateful)

- args: `mob_name: String`; `limit: Option<usize>`
- returns: `Vec<ZoneEncounterDto>`

## `list_mobs` (stateful)

- args: none
- returns: `Vec<MobDto>`

## `list_pm_threads` (stateful)

- args: none
- returns: `Vec<PmThreadDto>`

## `list_ui_files` (stateful)

- args: none
- returns: `Result<Vec<uifiles::UiFileInfoDto>, String>`

## `list_zone_encounters` (stateful)
why: zone page's recent fights, keyed by `zone_id` not display name; cheap on its own, no damage/drops -- see `get_encounter_detail`

- args: `zone_id: String`; `limit: Option<usize>`
- returns: `Vec<ZoneEncounterDto>`

## `list_zone_versions` (stateful)

- args: `zone: String`
- returns: `Vec<Option<String>>`

## `list_zone_visits` (stateful)

- args: none
- returns: `Vec<ZoneVisitDto>`

## `load_spellbook_file` (stateful)
why: a real character's saved [SpellLoadouts] -- see spellbookfiles's own doc. `file` is one of list_ui_files's own "hotbuttons"-kind entries (the non-`UI_`-prefixed one, which is where loadouts live).

- args: `file: String`
- returns: `Result<spellbookfiles::SpellbookFileDto, String>`

## `locate_item` (stateful)
why: "where is my X" -- GdLink's own locate affordance, wherever an item name already renders in the app.

- args: `name: String`
- returns: `Vec<inventory::InventoryLocation>`

## `locate_overlay` (stateful)
why: "where did that window go" -- a click-through, semi-transparent, always-on-top widget is easy to lose track of, especially right after a reposition or a display change. Brings it to front (a click-through window never gets real focus/raise from clicking through it) and emits an event carrying the target widget; OverlayApp.svelte owns the actual flash effect and filters to its own identity -- see set_overlay_opacity's own doc on why the payload carries the widget now instead of trusting emit_to alone. No-op if the widget's window isn't open.

- args: `widget: String`
- returns: `()`

## `reset_session` (stateful)

- args: none
- returns: `SessionDto`

## `resolve_spellbook_spell_ids` (stateful)
why: resolves a batch of catalog spell names to their real numeric ids in one call -- None per entry means spells_us.txt has no exact-name entry, not that the frontend did anything wrong. Batched on purpose: filling a loadout's worth of empty slots used to call this once per slot, each a fresh full spells_us.txt reparse -- real, measured slowdown. See spellbookfiles::resolve_spell_ids.

- args: `names: Vec<String>`
- returns: `Result<Vec<Option<i64>>, String>`

## `restart_app` (stateful)
why: Settings module's update-channel toggle -- see `updater`'s own doc

- args: none
- returns: `()`

## `restart_app` (stateful)

- args: none
- returns: `()`

## `save_spellbook_file` (stateful)

- args: `file: String`; `loadouts: Vec<spellbookfiles::SpellLoadoutDto>`
- returns: `Result<(), String>`

## `save_spellbook_file_as` (stateful)
why: real "save as" -- forks `source_file`'s pair (hotbuttons + its `UI_` layout counterpart) under a new `<Character>_<Zone>` stem, never touching the source. See spellbookfiles::save_spellbook_as.

- args: `source_file: String`; `new_stem: String`; `loadouts: Vec<spellbookfiles::SpellLoadoutDto>`
- returns: `Result<String, String>`

## `set_log_directory` (stateful)
why: native folder picker, None on cancel (not an error). Async callback API, not blocking_pick_folder -- Linux's GTK/portal dialog doesn't reliably mesh with a blocked command thread.

- args: `path: String`
- returns: `Result<StatusDto, String>`

## `set_log_directory` (stateful)
why: persists the base install folder, (re)starts the tail worker; same path for first-launch and "change folder" later. Worker points at `cfg.log_dir()`, not `path` directly.

- args: `path: String`
- returns: `Result<StatusDto, String>`

## `set_notification_enabled` (stateful)

- args: `kind: String`; `on: bool`
- returns: `Result<settings::NotificationSettings, String>`

## `set_overlay_enabled` (stateful)

- args: `widget: String`; `enabled: bool`
- returns: `Result<(), String>`

## `set_overlay_locked` (stateful)

- args: `widget: String`; `locked: bool`
- returns: `Result<(), String>`

## `set_overlay_opacity` (stateful)
why: live-pushes to this widget's own open window -- a no-op, not an error, when it isn't open; persistence is the caller's own setPreferences call.  emit_to alone does NOT scope this the way its own doc implies -- real bug, caught live: with every overlay window covered by the same "overlay-*" capability glob (capabilities/default.json), emit_to's permission check treats the whole glob as one audience and delivers to every window matching it, not just the one whose label was passed in. Confirmed with temporary two-sided logging: the SEND side always carried the right label, but every open overlay-* window's own listener fired regardless. So every payload here now carries the target widget too, and each window filters to its own identity (currentOverlayWidget()) before acting -- correct regardless of whatever emit_to does under the hood, not dependent on understanding its exact scoping behavior for this capability shape.

- args: `widget: String`; `opacity: f64`
- returns: `()`

## `set_overlay_overall_opacity` (stateful)
why: the SEPARATE "everything" fade (see preferences::default_overall_opacity's doc) -- same live-push/ persistence split as set_overlay_opacity, its own event name so the overlay window can tell the two apart. Same widget-in-payload filtering as set_overlay_opacity -- see its own doc.

- args: `widget: String`; `opacity: f64`
- returns: `()`

## `set_overlay_size` (stateful)
why: same live-push/persist split as set_overlay_opacity, but resizes the real OS window instead of a CSS value -- this command only forwards the raw string, it never touches actual pixel dimensions. The window receiving it is the one that already knows its own new size (OverlayApp.svelte's own 'overlay-size' listener, via ccSize. ts's own table) and resizes itself; only CC Tracker uses this today, shaped generically (a `widget` param, same as every other overlay setting) so the next widget with a size preset doesn't need a new command. Same widget-in-payload filtering as set_overlay_opacity -- see its own doc.

- args: `widget: String`; `size: String`
- returns: `()`

## `set_planner_state` (stateful)
why: whole-state write -- the frontend owns the merge (it knows which edit happened); race None clears, empty levels clears (the "Estimate levels" reset path).

- args: `race: Option<String>`; `levels: HashMap<String, u8>`
- returns: `Result<(), String>`

## `set_preferences` (stateful)

- args: `mut prefs: Preferences`
- returns: `Result<Preferences, String>`
